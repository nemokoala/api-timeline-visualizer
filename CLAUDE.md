# CLAUDE.md

## 작업 규칙

### 작업 완료 후 항상 빌드

코드 변경 작업을 마치면 사용자가 별도로 요청하지 않아도 반드시 `npm run build`를 실행한다.
(`build` = `tsc && vite build` — 타입 체크와 번들링을 함께 수행)

빌드가 실패하면 작업이 끝난 것이 아니다. 에러를 수정하고 빌드가 통과할 때까지 반복한 뒤,
그 결과를 사용자에게 보고한다.

### 작업 완료 후 문서 갱신

빌드가 통과하면, 사용자가 별도로 요청하지 않아도 아래 두 문서가 이번 변경으로 낡지 않았는지
확인하고 필요하면 같은 커밋에 함께 고친다. 해당 사항이 없으면 그냥 넘어간다.

**`docs/feature-ideas.md`**

- 목록에 있던 제안을 구현했으면 그 항목을 본문에서 빼고 `완료됨` 섹션으로 옮긴다.
  무엇을 왜 그렇게 만들었는지, 갈렸던 판단은 무엇인지 적는다. 항목을 빼면 남은 항목의 번호를
  1부터 다시 매긴다.
- 제안대로 하지 않고 다르게 풀었으면 그 사실과 이유를 적는다. 제안 문구를 그대로 두어
  이미 한 일처럼 보이게 하지 않는다.
- 작업 중 발견했지만 이번에 처리하지 않은 빚은 `후속 정리 대상`에 남긴다. 반대로 그 빚을
  갚았으면 해당 항목을 지운다.
- 목록에 없던 버그를 고쳤어도 `완료됨`에 기록한다 — 원인과 재현 조건까지.
- 남은 항목의 **근거**(파일·줄 번호)가 이번 변경으로 어긋났으면 고친다. 다른 항목이 이번
  구현 때문에 되살아날 수 있는 함정을 안게 됐다면 그 항목에 주의 문구를 단다.

**`CLAUDE.md`**

- 새로 생긴 공통 규칙·명령·컨벤션이 있으면 적는다.
- 여기 적힌 규칙이 코드와 어긋나게 됐으면 고친다.
- 한 번 쓰고 마는 구현 세부는 적지 않는다. 코드가 이미 말해 주는 것도 적지 않는다.
